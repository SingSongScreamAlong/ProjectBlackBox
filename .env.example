# ProjectBlackBox Environment Configuration
# Copy this file to .env and fill in your API keys

# ============================================================================
# CENTRALIZED API KEYS (for testing/distribution)
# ============================================================================
# These keys will be used by all users unless they configure their own
# in the settings manager. This makes testing and distribution easier.

# OpenAI API Key (for speech-to-text via Whisper)
OPENAI_API_KEY=your_openai_api_key_here

# ElevenLabs API Key (for text-to-speech)
ELEVENLABS_API_KEY=your_elevenlabs_api_key_here

# ============================================================================
# DATABASE (optional - defaults work for local Docker setup)
# ============================================================================
# DATABASE_URL=postgresql://blackbox:blackbox@localhost:5432/blackbox

# ============================================================================
# NOTES
# ============================================================================
# - Users can still override these by running: python settings_manager.py
# - Environment variables take precedence over user settings
# - For production, consider using a subscription model to cover API costs
