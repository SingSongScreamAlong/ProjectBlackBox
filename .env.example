# ============================================================================
# ProjectBlackBox Server Configuration
# ============================================================================
# FOR SERVER OPERATORS ONLY - Users do not need to configure anything
# Copy this file to .env: cp .env.example .env
# ============================================================================

# ----------------------------------------------------------------------------
# BLACKBOX API KEYS (Server-side only - never exposed to users)
# ----------------------------------------------------------------------------
# These keys are used by the BlackBox server to provide AI features to all users.
# Users connect to your server and use these keys through the API.

# OpenAI API Key - Powers AI coaching and speech-to-text for all users
OPENAI_API_KEY=sk-your-openai-key-here

# ElevenLabs API Key - Powers voice synthesis for all users  
ELEVENLABS_API_KEY=your-elevenlabs-key-here

# ----------------------------------------------------------------------------
# DATABASE (Optional - defaults work for local development)
# ----------------------------------------------------------------------------
DATABASE_URL=postgresql://blackbox:blackbox@localhost:5432/blackbox
PG_CONNECTION_STRING=postgresql://blackbox:blackbox@localhost:5432/blackbox

# ----------------------------------------------------------------------------
# SECURITY (Change in production!)
# ----------------------------------------------------------------------------
JWT_SECRET=change-this-to-a-random-32-character-string

# ----------------------------------------------------------------------------
# SERVER CONFIGURATION (Optional - defaults work for local development)
# ----------------------------------------------------------------------------
NODE_ENV=development
PORT=3000
SERVER_URL=http://localhost:3000
BACKEND_URL=http://localhost:3000
RELAY_AGENT_URL=ws://localhost:8765
DASHBOARD_URL=http://localhost:3001

# ----------------------------------------------------------------------------
# FEATURE FLAGS (Optional)
# ----------------------------------------------------------------------------
ENABLE_AI_COACHING=true
ENABLE_VOICE_SYNTHESIS=true

# ----------------------------------------------------------------------------
# RELAY AGENT CONFIGURATION (For Windows users)
# ----------------------------------------------------------------------------
# Set this to your DigitalOcean deployment URL for cloud mode
# Leave as localhost for local development
BLACKBOX_SERVER_URL=http://localhost:3000

# For DigitalOcean deployment, use:
# BLACKBOX_SERVER_URL=https://blackbox-racing-xxxxx.ondigitalocean.app

# ----------------------------------------------------------------------------
# NOTES
# ----------------------------------------------------------------------------
# - This single .env file is used by both the server and relay agent
# - Never commit this file to git (it's in .gitignore)
# - For production, use strong random values for JWT_SECRET
# - For DigitalOcean deployment, see DEPLOY.md
